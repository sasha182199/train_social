let initialState = {
    FriendsList : [
        {name : "Alex", id : 3},
        {name : "Youvelir", id : 4},
        {name : "MilRip", id : 5}
    ]
}

const sidebarReducer = (state = initialState, action) => {

    return state;
}

export default sidebarReducer;